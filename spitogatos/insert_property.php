<?php
// Σύνδεση στη βάση δεδομένων
// Υποθέτουμε ότι έχουμε ήδη δημιουργήσει τη σύνδεση στη βάση δεδομένων

// Έλεγχος εάν υπάρχουν δεδομένα που υποβλήθηκαν από τη φόρμα
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    if (!empty($_POST['area']) && !empty($_POST['price']) && !empty($_POST['availability']) && !empty($_POST['area_size'])) {
        // Έλεγχος των τιμών για αποδοχή
        $area = $_POST['area'];
        $price = $_POST['price'];
        $availability = $_POST['availability'];
        $area_size = $_POST['area_size'];

        // Εκτέλεση ενδεδειγμένων ελέγχων για τις τιμές

        // Εισαγωγή δεδομένων στη βάση δεδομένων μετά την επιτυχή επικύρωση
    } else {
        // Αν κάποιο πεδίο λείπει, εμφανίζεται μήνυμα σφάλματος
        echo "Όλα τα πεδία είναι υποχρεωτικά.";
    }
}

